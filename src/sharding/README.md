# Модуль шардинга

Здесь реализованы структуры и функции для распределения состояния и обработки сообщений по шардам (shards).

- `Shard` — структура для отдельного шарда (подсети).
- `assign_shard` — функция для распределения сообщений по шардам по хешу state_id. 