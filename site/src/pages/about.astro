---
import BaseLayout from '@layouts/BaseLayout.astro';
const baseRaw = import.meta.env.BASE_URL || '/';
const base = baseRaw.endsWith('/') ? baseRaw : baseRaw + '/';
const withBase = (path: string) => base + path.replace(/^\/+/, '');
---
<BaseLayout title="About — TRIAD">
  <section class="relative mx-auto max-w-6xl px-4 py-12">
    <div class="pointer-events-none absolute inset-0 opacity-40 [mask-image:radial-gradient(70%_60%_at_50%_30%,black,transparent)]">
      <div class="absolute -inset-24 bg-[radial-gradient(circle_at_top_right,rgba(124,156,255,0.20),transparent_60%)]"></div>
      <div class="absolute -inset-24 bg-[radial-gradient(circle_at_bottom_left,rgba(0,255,200,0.12),transparent_60%)]"></div>
    </div>

    <div class="relative">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight">TRIAD — a quantum‑native L1 architecture</h1>
      <p class="mt-3 text-white/80 text-base md:text-lg max-w-3xl">
        TRIAD started as a classical blockchain: we experimented with dozens of consensus mechanisms (40+ combinations) and
        used ML to optimize network parameters and routing. After studying the impact of quantum computers on standard crypto schemes,
        we pivoted: instead of treating quantum as a threat, we embraced it as the core of the design.
      </p>

      <div class="mt-10 grid md:grid-cols-12 gap-8">
        <div class="md:col-span-7 space-y-8 text-mist leading-relaxed">
          <div class="space-y-3">
            <h2 class="text-xl font-semibold text-white">The TRIAD story</h2>
            <ol class="relative border-l border-white/10 ml-2 pl-6 space-y-6">
              <li>
                <div class="absolute -left-[7px] mt-1 h-3 w-3 rounded-full bg-sky-400 shadow-[0_0_0_3px_rgba(124,156,255,0.2)]"></div>
                <h3 class="text-white font-medium">Phase 1 — Classical architecture</h3>
                <p class="mt-1">Exploring PoS/HotStuff/Tendermint‑like schemes, hybrids, mempool queuing, and global ordering.</p>
              </li>
              <li>
                <div class="absolute -left-[7px] mt-1 h-3 w-3 rounded-full bg-emerald-400 shadow-[0_0_0_3px_rgba(0,255,200,0.18)]"></div>
                <h3 class="text-white font-medium">Phase 2 — ML as an optimizer</h3>
                <p class="mt-1">ML heuristics for network parameters, load prediction, adaptive message routing, and finality heuristics.</p>
              </li>
              <li>
                <div class="absolute -left-[7px] mt-1 h-3 w-3 rounded-full bg-fuchsia-400 shadow-[0_0_0_3px_rgba(255,0,200,0.18)]"></div>
                <h3 class="text-white font-medium">Pivot — Quantum threat as opportunity</h3>
                <p class="mt-1">After analyzing research on quantum risks for classical blockchains, we reframed the problem: not to bypass the threat, but to build a system where quantum effects are the basis of consensus.</p>
              </li>
              <li>
                <div class="absolute -left-[7px] mt-1 h-3 w-3 rounded-full bg-indigo-400 shadow-[0_0_0_3px_rgba(99,102,241,0.18)]"></div>
                <h3 class="text-white font-medium">Phase 3 — TRIAD as a physical model</h3>
                <p class="mt-1">Transactions are "waves" with amplitude, phase, and TTL. We analyze interference across many parallel shards and decide where constructive interference emerges.</p>
              </li>
            </ol>
          </div>

          <div class="space-y-3">
            <h2 class="text-xl font-semibold text-white">Why it matters</h2>
            <ul class="grid gap-3">
              <li class="rounded-lg border border-white/10 bg-white/5 p-4">
                <b class="text-white">Quantum‑aware resilience:</b> rethinking consensus reduces reliance on heavy global ordering and makes the system adaptable to future crypto changes.
              </li>
              <li class="rounded-lg border border-white/10 bg-white/5 p-4">
                <b class="text-white">Parallelism by default:</b> DAG structure and batch computation (incl. Ed25519) fully utilize CPU/GPU resources.
              </li>
              <li class="rounded-lg border border-white/10 bg-white/5 p-4">
                <b class="text-white">Finality from physics:</b> decisions emerge from interference, not voting bureaucracy.
              </li>
            </ul>
          </div>

          <blockquote class="border-l-2 border-white/20 pl-4 italic text-white/85">
            Our goal is to lay the foundation for an L1 where performance is defined by computational efficiency and the laws of nature —
            not by the number of votes or queue sizes.
          </blockquote>
        </div>

        <aside class="md:col-span-5 space-y-4">
          <div class="rounded-xl border border-white/10 bg-white/5 p-5">
            <h3 class="text-white font-semibold">Where to go next</h3>
            <p class="mt-1 text-mist">See unique solutions and animations in the “Innovations” section.</p>
            <a href={withBase('innovations')} class="mt-3 inline-flex items-center gap-2 rounded-lg bg-white/10 px-4 py-2 text-white hover:bg-white/15 transition">
              Go to Innovations →
            </a>
          </div>

          <div class="rounded-xl border border-white/10 bg-white/5 p-5">
            <h3 class="text-white font-semibold">Core principles</h3>
            <ul class="mt-2 list-disc pl-5 space-y-1 text-mist">
              <li>Interference over voting</li>
              <li>DAG and minimal global ordering</li>
              <li>Batch cryptography, SIMD, multithreading</li>
              <li>Ready for hardware acceleration</li>
            </ul>
          </div>
        </aside>
      </div>
    </div>
  </section>
</BaseLayout>
