---
import logoUrl from '../assets/Untitled design-2.svg?url';
const baseRaw = import.meta.env.BASE_URL || '/';
const base = baseRaw.endsWith('/') ? baseRaw : baseRaw + '/';
const withBase = (path: string) => base + path.replace(/^\/+/, '');
const logoHref = logoUrl;
---
<section class="relative overflow-hidden">
  <div class="mx-auto max-w-6xl px-4 py-16 md:py-24 grid md:grid-cols-2 gap-10 items-center">
    <div>
      <img src={logoHref} alt="TRIAD logo" class="h-16 md:h-20 w-auto animate-glow animate-float" decoding="async" fetchpriority="high" />
      <h1 class="mt-4 text-3xl md:text-5xl font-bold tracking-tight animate-slide-up">Мы превращаем транзакции в волны.</h1>
      <p class="mt-4 text-mist text-lg animate-fade-in">Консенсус — это их интерференция. Это не протокол. Это физика.</p>
      <div class="mt-8 flex items-center gap-3 animate-fade-in">
        <a href={withBase('start')} class="px-5 py-2.5 rounded-md bg-white text-ink font-semibold hover:brightness-95 active:translate-y-px">Быстрый старт</a>
        <a href={withBase('about')} class="px-5 py-2.5 rounded-md border border-white/20 text-white hover:bg-white/10 active:translate-y-px">Подробнее о проекте</a>
      </div>
      <p class="mt-4 text-sm text-mist">“TRIAD consensus isn’t a protocol — it’s physics.”</p>
    </div>
    <div class="relative animate-fade-in"></div>
  </div>
</section>
